var app = app || {};
app.SetsView = Backbone.View.extend({
  el: '#sets',

  //leaving out initialize for now
  
  //eventually - grab all members of the collection and render them individually
  render: functon() {
    return this;
  }

  //renderSet function comes in here
});
